debug:
  logger:
    level: debug
    directory: data/logs/
    max_size: 10
    max_age: 30
  database:
    directory: data/badger/
  http:
    port: 8080

release:
  logger:
    level: warn
    directory: data/logs/
    max_size: 10
    max_age: 30
  database:
    directory: data/badger/
  http:
    port: 8080
